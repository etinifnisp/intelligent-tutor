# Logs
*.log
*.zip
frontend_backup/

# Runtime data
data/graph_store.json
data/learner_memory.json
data/app.db

# Papers (large PDFs — keep locally, not in git)
data/papers/**/*.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Env
.env
backend/.env
.env.local
backend/.env.local

# Local docs / reports (not for git)
intelligent_jee_tutor_production_audit_local_hackathon.md
impact-statement.pdf
hardware_profile.md
hackathon_master_document.md
baseline_report.md
scripts/generate_impact_pdf.py

# Cursor IDE
.cursor/

# Frozen corpus copy (manifest is committed; JSON is regenerated locally)
data/corpus/frozen/jee_corpus_v1_baseline.json

# Corpus pipeline outputs (Phase 2) — generated locally
data/corpus/paper_registry.json

# Retrieval indexes (Phase 4) — generated locally
data/indexes/*
!data/indexes/.gitkeep
